<section id="editbooking">
	<h2><%= @header %></h2>
	<%= render "error_messages", :target => @booking %>
	<%= render "form", :header => @header %>
	<% if allow? :bookings, :destroy, @booking %>
    	<div><%= link_to "Delete Booking", booking_path(@booking), method: :delete %></div>
  	<% end %>
</section>