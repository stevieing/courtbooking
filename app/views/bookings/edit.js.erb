$("#ajax-booking").addDialog("<%= escape_javascript(render('form_ajax', header: @header)) %>", "Edit Booking");
$("#ajax-booking").append("<%= escape_javascript(render('delete', booking: @booking)) %>");
$('#booking_opponent_name').addAutocomplete();